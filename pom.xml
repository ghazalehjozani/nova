<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>ir.dotin.loan</groupId>
  <artifactId>morabehe-loan</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>Morabehe Loan :: Parent</name>
  <packaging>pom</packaging>

  <modules>
    <module>core</module>
    <module>adapters</module>
    <module>container</module>
  </modules>

  <properties>
    <loan-dependencies.version>0.0.1-SNAPSHOT</loan-dependencies.version>
    <loan-dependencies-morabehe.version>0.0.1-SNAPSHOT</loan-dependencies-morabehe.version>

    <java.version>21</java.version>
    <maven.compiler.source>${java.version}</maven.compiler.source>
    <maven.compiler.target>${java.version}</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <maven.compiler-plugin.version>3.12.1</maven.compiler-plugin.version>
    <maven-source-plugin.version>3.3.1</maven-source-plugin.version>

    <jacoco.version>0.8.12</jacoco.version>
    <maven-source-plugin.version>3.3.1</maven-source-plugin.version>
    <maven-surefire.version>2.22.0</maven-surefire.version>
    <maven-failsafe.version>2.22.0</maven-failsafe.version>

    <sonar.projectKey>ir.dotin.loan:morabehe-loan</sonar.projectKey>
    <sonar.projectName>Morabehe Loan</sonar.projectName>
    <sonar.projectVersion>${project.version}</sonar.projectVersion>
    <sonar.qualitygate.wait>true</sonar.qualitygate.wait>
    <sonar.language>java</sonar.language>
    <sonar.sourceEncoding>UTF-8</sonar.sourceEncoding>
    <sonar.sources>src</sonar.sources>
    <sonar.dynamicAnalysis>reuseReports</sonar.dynamicAnalysis>
    <sonar.java.coveragePlugin>jacoco</sonar.java.coveragePlugin>
    <sonar.sources>src/main</sonar.sources>
    <sonar.tests>src/test/java</sonar.tests>
    <sonar.binaries>target/classes</sonar.binaries>
    <sonar.coverage.jacoco.xmlReportPaths>target/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>ir.dotin.loan</groupId>
        <artifactId>loan-dependencies</artifactId>
        <version>${loan-dependencies.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>ir.dotin.loan</groupId>
        <artifactId>loan-dependencies-morabehe</artifactId>
        <version>${loan-dependencies-morabehe.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler-plugin.version}</version>
      </plugin>
    </plugins>
  </build>

</project>
