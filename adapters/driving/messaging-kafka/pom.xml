<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>ir.dotin.loan</groupId>
    <artifactId>trade-loan-adapters-driving</artifactId>
    <version>${revision}</version>
  </parent>

  <artifactId>trade-loan-adapters-driving-messaging-kafka</artifactId>
  <name>Trade Loan :: Adapters :: Driving :: Messaging (Kafka)</name>
  <packaging>jar</packaging>

    <dependencies>
        <!-- Platform -->
        <dependency>
            <groupId>ir.dotin.platform</groupId>
            <artifactId>platform-messaging-api</artifactId>
        </dependency>
        <dependency>
            <groupId>ir.dotin.platform</groupId>
            <artifactId>platform-messaging-core</artifactId>
        </dependency>
        <dependency>
            <groupId>ir.dotin.platform</groupId>
            <artifactId>platform-inbox-core</artifactId>
        </dependency>
        <dependency>
            <groupId>ir.dotin.platform</groupId>
            <artifactId>platform-spring-boot-starter-messaging-kafka</artifactId>
        </dependency>


        <!-- Sibling module -->
        <dependency>
            <groupId>ir.dotin.loan</groupId>
            <artifactId>trade-loan-adapters-driving-contract</artifactId>
        </dependency>

        <!-- AsyncAPI documentation -->
        <dependency>
            <groupId>ir.dotin.platform</groupId>
            <artifactId>platform-spring-boot-starter-asyncapi</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.springwolf</groupId>
            <artifactId>springwolf-kafka</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.springwolf</groupId>
            <artifactId>springwolf-ui</artifactId>
        </dependency>

        <dependency>
            <groupId>net.time4j</groupId>
            <artifactId>time4j-base</artifactId>
            <version>5.9.4</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Tracing API for FCB-legacy attribute tagging on inbound consumer spans -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing</artifactId>
        </dependency>
    </dependencies>
</project>
