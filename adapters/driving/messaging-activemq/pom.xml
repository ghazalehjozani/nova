<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>ir.dotin.loan</groupId>
    <artifactId>trade-loan-adapters-driving</artifactId>
    <version>${revision}</version>
  </parent>

  <artifactId>trade-loan-adapters-driving-messaging-activemq</artifactId>
  <name>Trade Loan :: Adapters :: Driving :: Messaging (ActiveMQ)</name>
  <packaging>jar</packaging>

    <dependencies>
        <!-- Platform -->
        <dependency>
            <groupId>ir.dotin.platform</groupId>
            <artifactId>platform-messaging-api</artifactId>
        </dependency>
        <dependency>
            <groupId>ir.dotin.platform</groupId>
            <artifactId>platform-messaging-core</artifactId>
        </dependency>
        <dependency>
            <groupId>ir.dotin.platform</groupId>
            <artifactId>platform-spring-boot-starter-messaging-activemq</artifactId>
        </dependency>

        <!-- Sibling module -->
        <dependency>
            <groupId>ir.dotin.loan</groupId>
            <artifactId>trade-loan-adapters-driving-contract</artifactId>
        </dependency>

        <!-- Spring / JMS (provided by platform starter, explicit for clarity) -->
        <dependency>
            <groupId>jakarta.jms</groupId>
            <artifactId>jakarta.jms-api</artifactId>
        </dependency>

        <!-- pooled-jms now provided by platform starter -->

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>
