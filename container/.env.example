# =============================================================================
# .env.example — copy to .env (git-ignored) and fill in real values for local dev
# -----------------------------------------------------------------------------
# In production, these are injected by the orchestrator / secrets manager.
# Nothing here is ever committed. Nothing here is ever stored in Consul.
# =============================================================================

# --- Spring profile -----------------------------------------------------------
SPRING_PROFILES_ACTIVE=dev, dev-elk, kafka-fcb, elk

# --- Consul connection --------------------------------------------------------
CONSUL_HOST=consul.nova.local
CONSUL_PORT=8500
CONSUL_SCHEME=http
CONSUL_ACL_TOKEN=
CONSUL_TLS_ENABLED=false
CONSUL_TLS_CERT_PATH=
CONSUL_TLS_TRUSTSTORE_TYPE=JKS
CONSUL_TLS_TRUSTSTORE_PASSWORD=

# --- Datasource ---------------------------------------------------------------
DB_HOST=postgres.nova.local
DB_PORT=5432
DB_NAME=nova
DB_USERNAME=
DB_PASSWORD=

# --- Redis --------------------------------------------------------------------
REDIS_HOST=redis.nova.local
REDIS_PASSWORD=

# --- Kafka --------------------------------------------------------------------
KAFKA_BOOTSTRAP_SERVERS=kafka-1:9092,kafka-2:9092,kafka-3:9092
KAFKA_USER=
KAFKA_PASSWORD=
KAFKA_SECURITY_PROTOCOL=SASL_PLAINTEXT
KAFKA_SASL_MECHANISM=SCRAM-SHA-256
KAFKA_CONSUMER_GROUP=core.loan.nova.fcb-integration.v1

# --- ActiveMQ -----------------------------------------------------------------
ACTIVEMQ_HOST=activemq.nova.local
ACTIVEMQ_PORT=61616
ACTIVEMQ_USER=
ACTIVEMQ_PASSWORD=

# --- FCB integration (Deprecated Usecase runner) ----------------------------------------------------------
FCB_BASE_URL=
FCB_USERNAME=
FCB_PASSWORD=

# --- OAuth2 / OIDC ------------------------------------------------------------
OAUTH2_CLIENT_ID=
OAUTH2_CLIENT_SECRET=

# --- Observability ------------------------------------------------------------
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://otel-collector:4318/v1/traces
OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://otel-collector:4318/v1/metrics
OTEL_TRACES_SAMPLER_ARG=0.2
OTEL_SERVICE_NAMESPACE=nova
ELK_ENABLED=false
ELK_LOG_TOPIC=corridor.core.loan.nova.obs.logs.v1
ELK_APM_OTLP_ENDPOINT=http://apm-server:8200/v1/traces

# --- Platform -----------------------------------------------------------------
PLATFORM_ENV=dev
HIBERNATE_DDL_AUTO=validate
SHOW_SQL=false
