# =============================================================================
# .env.example — copy to .env (git-ignored) and fill in real values for local dev
# -----------------------------------------------------------------------------
# This file holds ONLY what the Nova app reads at runtime. The local infra containers
# (postgres, redis HA, broker) and their docker-compose-only env now live in the sibling
# repo nova-dev-stack/. A few connection secrets are duplicated across both files — keep in sync:
#   DB_NAME, DB_USERNAME, DB_PASSWORD, REDIS_PASSWORD, REDIS_MASTER_NAME, ARTEMIS_USER, ARTEMIS_PASSWORD
# In production these are injected by the orchestrator / secrets manager.
# Nothing here is ever committed. Nothing here is ever stored in Consul.
# =============================================================================

# --- Spring profile -----------------------------------------------------------
SPRING_PROFILES_ACTIVE=dev

# --- Consul connection --------------------------------------------------------
CONSUL_HOST=consul.nova.local
CONSUL_PORT=8500
CONSUL_SCHEME=http
CONSUL_ACL_TOKEN=
CONSUL_TLS_ENABLED=false
CONSUL_TLS_CERT_PATH=
CONSUL_TLS_TRUSTSTORE_TYPE=JKS
CONSUL_TLS_TRUSTSTORE_PASSWORD=

# --- Datasource (client). DB_NAME/USERNAME/PASSWORD duplicated in nova-dev-stack/.env -----------
DB_HOST=postgres.nova.local
DB_PORT=5432
DB_NAME=nova
DB_USERNAME=
DB_PASSWORD=

# --- Redis client (Sentinel HA discovery) ------------------------------------
# REDIS_PASSWORD / REDIS_MASTER_NAME are duplicated in nova-dev-stack/.env (redis server-side) —
# keep both in sync. The local redis containers, their host port mappings, and the sentinel announce
# topology now live in nova-dev-stack/.env.example (docker-compose bootstrap concern, not the app).
REDIS_MASTER_NAME=nova-master
REDIS_PASSWORD=
REDIS_TLS_ENABLED=false

# Sentinel endpoints the app uses to discover the topology.
REDIS_SENTINEL_NODES=host.docker.internal:26379,host.docker.internal:26380,host.docker.internal:26381

# --- Kafka --------------------------------------------------------------------
KAFKA_BOOTSTRAP_SERVERS=kafka-1:9092,kafka-2:9092,kafka-3:9092
KAFKA_USER=
KAFKA_PASSWORD=
KAFKA_SECURITY_PROTOCOL=SASL_PLAINTEXT
KAFKA_SASL_MECHANISM=SCRAM-SHA-256
KAFKA_CONSUMER_GROUP=core.loan.nova.fcb-integration.v1

# --- FCB Artemis broker connection (client; address/user/pass ONLY) ---
# ARTEMIS_USER/PASSWORD duplicated in nova-dev-stack/.env (broker bootstrap) — keep in sync.
# The active transport (transport-mode) + artemis.enabled live in Consul/nova-config, NOT here.
ACTIVEMQ_HOST=artemis.nova.local
ACTIVEMQ_PORT=61616
ARTEMIS_USER=
ARTEMIS_PASSWORD=

# --- FCB integration (Deprecated Usecase runner) ----------------------------------------------------------
FCB_BASE_URL=
FCB_USERNAME=
FCB_PASSWORD=

# --- OAuth2 / OIDC ------------------------------------------------------------
OAUTH2_CLIENT_ID=
OAUTH2_CLIENT_SECRET==

ENVELOPE_SIGNER_PRIVATE_KEY_REF=file:/home/m.amirabdollahi/workspaces/trade-loan/container/keystore.p12
ENVELOPE_KEYSTORE_PASSWORD=changeit
ENVELOPE_KEY_PASSWORD=changeit

# --- Observability (OpenTelemetry) -------------------------------------------
OTEL_EXPORTER_OTLP_ENDPOINT=http://10.100.7.20:4318
OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
OTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer Unmasking-Activist5-Unsecured
OTEL_AUTH_HEADER=Bearer Unmasking-Activist5-Unsecured
OTEL_EXPORTER_OTLP_COMPRESSION=gzip
OTEL_EXPORTER_OTLP_TIMEOUT=10000
OTEL_LOGS_EXPORTER=otlp
OTEL_METRICS_EXPORTER=otlp
OTEL_TRACES_EXPORTER=otlp
OTEL_METRIC_EXPORT_INTERVAL=30000
OTEL_TRACES_SAMPLER=parentbased_traceidratio
OTEL_TRACES_SAMPLER_ARG=0.2
OTEL_SERVICE_NAME=nova-service
OTEL_SERVICE_NAMESPACE=nova
OTEL_PROPAGATORS=tracecontext,baggage
KUBERNETES_NAMESPACE=
KUBERNETES_POD_NAME=
HOSTNAME=

# --- Platform -----------------------------------------------------------------
PLATFORM_ENV=dev
HIBERNATE_DDL_AUTO=validate
SHOW_SQL=false

SERVER_PORT=8080
