<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds" debug="false">

    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="nova-service"/>
    <springProperty scope="context" name="APP_VERSION" source="spring.application.version" defaultValue="unknown"/>
    <springProperty scope="context" name="ENV" source="platform.environment" defaultValue="dev"/>

    <property name="LOG_PATH" value="${LOG_PATH:-./logs}"/>
    <property name="HOSTNAME" value="${HOSTNAME:-localhost}"/>

    <conversionRule conversionWord="clr" class="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex" class="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx" class="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>

    <property name="CONSOLE_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%-5p){bold,highlight} %clr([trace=%X{traceId:-},span=%X{spanId:-}]){cyan} %clr([corr=%X{correlationId:-},user=%X{userId:-},tenant=%X{tenantId:-}]){magenta} %clr(%-40.40logger{39}){blue} : %m%n%clr(%wEx){red}"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
        <captureExperimentalAttributes>true</captureExperimentalAttributes>
        <captureCodeAttributes>true</captureCodeAttributes>
        <captureMarkerAttribute>true</captureMarkerAttribute>
        <captureKeyValuePairAttributes>true</captureKeyValuePairAttributes>
        <captureMdcAttributes>*</captureMdcAttributes>
    </appender>

    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}-error.log</file>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/${APP_NAME}-error-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="ASYNC_OTEL" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="OTEL"/>
        <queueSize>8192</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>true</neverBlock>
        <maxFlushTime>5000</maxFlushTime>
    </appender>

    <appender name="ASYNC_ERROR" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="ERROR_FILE"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>false</neverBlock>
        <maxFlushTime>10000</maxFlushTime>
    </appender>

    <logger name="ir.dotin.platform" level="INFO"/>
    <logger name="ir.dotin.loan" level="INFO"/>
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.springframework.boot" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.springframework.security" level="INFO"/>
    <logger name="org.springframework.kafka" level="WARN"/>
    <logger name="org.springframework.cloud.consul" level="INFO"/>
    <logger name="org.apache.kafka" level="WARN"/>
    <logger name="org.apache.kafka.clients" level="ERROR"/>
    <logger name="org.apache.kafka.clients.NetworkClient" level="ERROR"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.hibernate.SQL" level="INFO"/>
    <logger name="com.zaxxer.hikari" level="INFO"/>
    <logger name="org.postgresql" level="WARN"/>
    <logger name="io.opentelemetry" level="INFO"/>
    <logger name="io.micrometer" level="INFO"/>
    <logger name="io.micrometer.core.instrument" level="WARN"/>

    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_OTEL"/>
            <appender-ref ref="ASYNC_ERROR"/>
        </root>
        <logger name="ir.dotin" level="DEBUG"/>
        <logger name="org.springframework.security" level="INFO"/>
        <logger name="org.springframework.security.web.FilterChainProxy" level="WARN"/>
        <logger name="org.springframework.cloud.consul" level="INFO"/>
    </springProfile>

    <springProfile name="stage,prod">
        <appender name="STDOUT_JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>traceId</includeMdcKeyName>
                <includeMdcKeyName>spanId</includeMdcKeyName>
                <includeMdcKeyName>traceFlags</includeMdcKeyName>
                <includeMdcKeyName>correlationId</includeMdcKeyName>
                <includeMdcKeyName>requestId</includeMdcKeyName>
                <includeMdcKeyName>userId</includeMdcKeyName>
                <includeMdcKeyName>tenantId</includeMdcKeyName>
                <includeMdcKeyName>idempotencyKey</includeMdcKeyName>
                <includeMdcKeyName>messageType</includeMdcKeyName>
                <includeMdcKeyName>transport</includeMdcKeyName>
                <includeMdcKeyName>flow</includeMdcKeyName>
                <customFields>{"service.name":"${APP_NAME}","service.version":"${APP_VERSION}","deployment.environment":"${ENV}","host.name":"${HOSTNAME}"}</customFields>
            </encoder>
        </appender>
        <appender name="ASYNC_STDOUT_JSON" class="ch.qos.logback.classic.AsyncAppender">
            <appender-ref ref="STDOUT_JSON"/>
            <queueSize>2048</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <neverBlock>true</neverBlock>
        </appender>
        <root level="INFO">
            <appender-ref ref="ASYNC_STDOUT_JSON"/>
            <appender-ref ref="ASYNC_OTEL"/>
            <appender-ref ref="ASYNC_ERROR"/>
        </root>
    </springProfile>

    <springProfile name="!dev &amp; !stage &amp; !prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_OTEL"/>
        </root>
    </springProfile>

</configuration>
