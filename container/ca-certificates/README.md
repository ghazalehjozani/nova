# Drop your internal CA `.crt` files in this directory.
# The Dockerfile picks them up automatically and imports them into both the
# OS trust store and the Java truststore. Empty by default — that's fine for dev.
